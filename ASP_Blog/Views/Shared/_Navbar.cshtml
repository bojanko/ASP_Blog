<nav class="navbar navbar-expand-lg navbar-dark bg-primary">

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item @(ViewBag.Page == "home" ? "active" : "")">
        <a class="nav-link" href="@Url.Action("Home", "Page", null)">Home</a>
      </li>
      <li class="nav-item @(ViewBag.Page == "about" ? "active" : "")">
        <a class="nav-link" href="@Url.Action("About", "Page", null)">About</a>
      </li>
      <li class="nav-item @(ViewBag.Page == "contact" ? "active" : "")">
        <a class="nav-link" href="@Url.Action("Contact", "Page", null)">Contact</a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Menu
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            @if (!User.Identity.IsAuthenticated)
            {
                <a class="dropdown-item" href="@Url.Action("Login", "Login", null)">Log In</a>
                <a class="dropdown-item" href="@Url.Action("Register", "Login", null)">Register</a>
            }

            @if (User.Identity.IsAuthenticated)
            {
                <a class="dropdown-item" href="#">Profile</a>
            
                if (User.IsInRole("ROLE_ADMIN"))
                {
                    <a class="dropdown-item" href="#">Admin panel</a>
                }
            
                <div class="dropdown-divider"></div>
            
                <a class="dropdown-item" href="@Url.Action("Logout", "Login", null)">Log Out</a>
            }
        </div>
      </li>
    </ul>
  </div>
</nav>
